{
  "scripts": {
    "test": "vitest test/compiler.spec.js",
    "testDebug": "vitest test/core.spec.js:4 --inspect-brk --no-file-parallelism --test-timeout=0",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.4",
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
